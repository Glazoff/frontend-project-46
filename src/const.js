const extensions = ['json', 'yaml', 'yml']
const newline = '\n'

const filedStatus = {
  NOT_DIFF: 'not diff',
  ADDED: 'added',
  DELETED: 'deleted',
  MODIFIED: 'modified',
}

export { extensions, filedStatus, newline }
